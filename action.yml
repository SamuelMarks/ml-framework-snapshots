name: "Generate ML Snapshots"
description: "Generates JSON API snapshots for ML frameworks and optionally creates a PR."

inputs:
  frameworks:
    description: "Comma-separated list of frameworks to scan (e.g., 'torch,jax')."
    required: true
    default: "torch,jax,tensorflow,keras,mlx,flax"
  out-dir:
    description: "Output directory for snapshots."
    required: true
    default: "./snapshots"

runs:
  using: "composite"
  steps:
    - name: Create required directories and set permissions
      shell: bash
      run: |
        sudo mkdir -p /Users/samuel/repos
        sudo chown -R $(whoami) /Users

    - name: Checkout ml-switcheroo-ir
      shell: bash
      run: git clone https://github.com/SamuelMarks/ml-switcheroo-ir /Users/samuel/repos/ml-switcheroo-ir

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: "3.9"

    - name: Install ml-framework-snapshots and frameworks
      shell: bash
      run: |
        pip install ".[frameworks]"

    - name: Generate Snapshots
      shell: bash
      run: |
        mkdir -p "${{ inputs.out-dir }}"
        ml_framework_snapshots capture --out-dir "${{ inputs.out-dir }}"
