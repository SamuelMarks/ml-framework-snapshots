[pytest]
addopts = --cov=src/ml_framework_snapshots --cov-report=term-missing --cov-branch
pythonpath = src
testpaths = tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    frameworks: marks tests requiring live heavy ML frameworks (deselect with '-m "not frameworks"')
